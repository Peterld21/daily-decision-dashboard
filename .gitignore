# macOS
.DS_Store
._*

# Local benchmark pipeline（仅 JSON 入库；不重提较大 CSV）

benchmark_return/market_prices_post2020_publish.csv
benchmark_return/cumulative_returns_since_*.html

# 误拷的 SQLite（不应进 Pages 仓库）

data/*.db

# Editors
.vscode/
.idea/
*.swp

# Logs / scratch
*.log
.cache/
logs/

# Node (not used now, defensive)
node_modules/
dist/
.parcel-cache/

# Local serving
.python-http-server.pid

# Local publish config (含 STOCKS 等用户私有偏好)
scripts/publish.config.sh

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
